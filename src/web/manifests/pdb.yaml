apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: contoso-test
  labels:
    app.kubernetes.io/name: contoso-test
    app.kubernetes.io/part-of: contoso-test-project
    kubernetes.azure.com/generator: devhub
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: contoso-test