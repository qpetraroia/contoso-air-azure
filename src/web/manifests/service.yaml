apiVersion: v1
kind: Service
metadata:
  name: contosotest
  namespace: contosoair
  labels:
    app.kubernetes.io/name: contosotest
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contosotest
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000