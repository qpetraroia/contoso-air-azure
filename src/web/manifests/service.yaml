apiVersion: v1
kind: Service
metadata:
  name: contoso-test
  namespace: namespace-workflow-1746497961928
  labels:
    app.kubernetes.io/name: contoso-test
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-test
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000